const U="https://brqurxainlsteygsqfcu.supabase.co",ADMIN="gohtinghong2004@gmail.com";const hash=async v=>{const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("")};export default async req=>{if(req.method!=="POST")return new Response("Method not allowed",{status:405});const key=Netlify.env.get("SUPABASE_SERVICE_ROLE_KEY"),token=req.headers.get("authorization");if(!key||!token)return new Response("未登录",{status:401});try{const auth=await fetch(U+"/auth/v1/user",{headers:{apikey:key,Authorization:token}}),user=await auth.json();if(!auth.ok||user.email!==ADMIN)return new Response("仅管理员可操作",{status:403});const {customer,pin}=await req.json(),name=String(customer||"").trim(),code=String(pin||"").trim();if(!name||!/^[0-9]{4,6}$/.test(code))return new Response("请输入客户名和 4–6 位 PIN",{status:400});const r=await fetch(U+"/rest/v1/customer_access?on_conflict=customer_name",{method:"POST",headers:{apikey:key,Authorization:"Bearer "+key,"Content-Type":"application/json",Prefer:"resolution=merge-duplicates,return=minimal"},body:JSON.stringify({customer_name:name,pin_hash:await hash(code),updated_at:new Date().toISOString()})});if(!r.ok)throw Error();return new Response(JSON.stringify({ok:true}),{headers:{"Content-Type":"application/json"}})}catch{return new Response("保存失败",{status:500})}};export const config={path:"/api/customer-pin"};