const U="https://brqurxainlsteygsqfcu.supabase.co";const h=async v=>{const b=await crypto.subtle.digest("SHA-256",new TextEncoder().encode(v));return [...new Uint8Array(b)].map(x=>x.toString(16).padStart(2,"0")).join("")};async function db(p,k,o={}){const r=await fetch(U+"/rest/v1/"+p,{...o,headers:{apikey:k,Authorization:"Bearer "+k,"Content-Type":"application/json",...(o.headers||{})}});if(!r.ok)throw Error(await r.text()||"数据库保存失败");return r}export default async req=>{if(req.method!=="POST")return new Response("Method not allowed",{status:405});const k=Netlify.env.get("SUPABASE_SERVICE_ROLE_KEY");if(!k)return new Response("暂不可用",{status:503});try{const {customer,pin,taskId,orderNo,status,trackingNo}=await req.json(),name=String(customer||"").trim();if(!name||!pin||!taskId||!orderNo||!["tk","return"].includes(status)||!String(trackingNo||"").trim())return new Response("资料不完整",{status:400});const access=await (await db("customer_access?select=pin_hash&customer_name=eq."+encodeURIComponent(name)+"&limit=1",k)).json();if(!access[0]||await h(String(pin))!==access[0].pin_hash)return new Response("PIN 不正确",{status:403});const rows=await (await db("tasks?select=customer_name,task_content&id=eq."+encodeURIComponent(taskId)+"&limit=1",k)).json();if(!rows[0]||rows[0].customer_name!==name)return new Response("订单不属于该客户",{status:403});let m={};try{m=JSON.parse(rows[0].task_content||"{}")}catch{m={type:rows[0].task_content||"order"}}m.type=m.type||"order";m.customer_actions=m.customer_actions||{};m.customer_actions[String(orderNo)]={status,tracking_no:String(trackingNo).trim(),updated_at:new Date().toISOString()};await db("tasks?id=eq."+encodeURIComponent(taskId),k,{method:"PATCH",headers:{Prefer:"return=minimal"},body:JSON.stringify({task_content:JSON.stringify(m)})});return new Response(JSON.stringify({ok:true}),{headers:{"Content-Type":"application/json"}})}catch(e){return new Response(e.message||"保存失败",{status:500})}};export const config={path:"/api/customer-action"};