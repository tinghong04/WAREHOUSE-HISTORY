const U="https://brqurxainlsteygsqfcu.supabase.co";
const cors={"Access-Control-Allow-Origin":"https://cheerful-tanuki-93ee22.netlify.app","Access-Control-Allow-Methods":"POST, OPTIONS","Access-Control-Allow-Headers":"Content-Type"};
export default async req=>{if(req.method==="OPTIONS")return new Response("",{headers:cors});const key=Netlify.env.get("SUPABASE_SERVICE_ROLE_KEY");try{const {deviceId}=await req.json(),id=String(deviceId||"");const r=await fetch(U+"/rest/v1/shipping_devices?select=device_id,bound&device_id=eq."+encodeURIComponent(id)+"&limit=1",{headers:{apikey:key,Authorization:"Bearer "+key}});if(!r.ok)throw Error();const rows=await r.json();return Response.json({bound:rows[0]?.bound===true},{headers:cors})}catch{return Response.json({bound:false},{headers:cors})}};
export const config={path:"/api/shipping-device-status"};